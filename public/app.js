const SOCKET_URL = window.POKER_SERVER_URL || 'https://gamb-eu6t.onrender.com';
const socket = io(SOCKET_URL,{transports:['websocket','polling'],reconnection:true,reconnectionAttempts:Infinity});
let state=null,roomCode='',me=null,toastTimer=null,selectedChess=null,refillPromptOpen=false;
const GAME_RULES={
  poker:'Bet, raise, or fold through Preflop → Flop → Turn → River. Best 5-card hand wins the pot. Blinds are posted automatically each hand.',
  blackjack:'Beat the dealer without going over 21. Place a bet, then Hit (take a card), Stand (keep your total), or Double (double bet, one card). Dealer stands on 17+. Blackjack pays 3:2.',
  roulette:'Pick a bet type and amount, then the host spins. Red/Black/Odd/Even/High/Low pay 1:1. You have 10 seconds between rounds in the lobby to place bets.',
  chess:'White moves first. Click your piece, then click a destination square. Only legal moves are accepted. Checkmate wins; stalemate is a draw.'
};
const $=s=>document.querySelector(s), $$=s=>[...document.querySelectorAll(s)];
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const savedName=()=>localStorage.getItem('gh_name')||'';
function rememberName(v){v=String(v||'').trim().slice(0,18);if(v)localStorage.setItem('gh_name',v);return v}
function toast(m){const t=$('#toast');t.textContent=m;t.classList.add('show');clearTimeout(toastTimer);toastTimer=setTimeout(()=>t.classList.remove('show'),2800)}
function showScreen(id){$$('.screen').forEach(x=>x.classList.remove('active'));$('#'+id).classList.add('active')}
function openModal(html){$('#modalContent').innerHTML=html;$('#modal').classList.remove('hidden')}
function closeModal(){$('#modal').classList.add('hidden')}
function call(event,payload){return new Promise(resolve=>socket.emit(event,payload||{},r=>resolve(r||{})))}
function mePlayer(){return state?.players.find(p=>p.id===state.me?.id)||state?.players.find(p=>p.id===me?.id)}
function renderConnection(ok){$('#connection').classList.toggle('online',ok);$('#connection').innerHTML=`<i></i> ${ok?'Connected':'Disconnected'}`}
function saveSession(){if(roomCode&&me?.reconnectToken)localStorage.setItem('gh_session',JSON.stringify({code:roomCode,reconnectToken:me.reconnectToken}))}
socket.on('connect',async()=>{renderConnection(true);const s=JSON.parse(localStorage.getItem('gh_session')||'null');if(s){const r=await call('resumeRoom',s);if(r.ok){roomCode=s.code;toast('Reconnected')}}});
socket.on('disconnect',()=>renderConnection(false));socket.on('connect_error',()=>renderConnection(false));
socket.on('state',s=>{state=s;roomCode=s.code||roomCode;me={...(me||{}),...(s.me||{})};if(s.me?.reconnectToken)saveSession();if(s.code){$('#roomCode').textContent=s.code;$('#gameRoom').textContent=s.code}renderAll();maybeOfferRefill()});
function renderAll(){if(!state)return;$('#playerCount').textContent=state.players.length;renderPlayers();renderChat();updateChipBalance();if(state.status==='IN_GAME'){showScreen('game');renderGame()}else{showScreen('lobby');renderLobby()}}
function renderLobby(){const mine=mePlayer();$$('.game-tab').forEach(b=>b.classList.toggle('active',b.dataset.game===state.game));const canHost=mine?.host;$('#startBtn').disabled=!canHost;$('#addBotBtn').disabled=!canHost;const startLabel=state.game==='poker'?'START POKER':state.game==='blackjack'?'START BLACKJACK':state.game==='roulette'?'OPEN ROULETTE':'START CHESS';$('#startBtn').textContent=startLabel;const rules=$('#gameRules');if(rules)rules.innerHTML=`<h4>HOW TO PLAY — ${state.game.toUpperCase()}</h4><p>${GAME_RULES[state.game]||''}</p>`}
function renderPlayers(){
 $('#playersList').innerHTML=state.players.map(p=>`<div class="player-row"><div class="avatar">${esc(p.name.slice(0,2).toUpperCase())}</div><div class="player-meta"><b>${esc(p.name)} ${p.host?'★':''}</b><span>${p.bot?'BOT · '+esc(p.botType):(p.connected?'CONNECTED':'DISCONNECTED')}</span></div><div class="chips">${p.chips.toLocaleString()}</div></div>`).join('');
 $('#gamePlayers').innerHTML=state.players.map(p=>`<div class="game-player ${state.turnPlayerId===p.id?'active':''}"><div class="gp-top"><b>${esc(p.name)}</b><span>${p.bot?'BOT':'HUMAN'}</span></div><div class="gp-sub">${p.chips.toLocaleString()} chips · ${esc(p.lastAction||'')}</div></div>`).join('')
}
function renderChat(){const html=(state.chat||[]).map(m=>`<div class="chat-line ${m.system?'system':''}">${m.system?'':`<b>${esc(m.name)}:</b>`}${esc(m.text)}</div>`).join('');$('#chatLog').innerHTML=html;$('#gameChat').innerHTML=html;['#chatLog','#gameChat'].forEach(s=>{const x=$(s);x.scrollTop=x.scrollHeight})}
function card(c){if(c==='??')return '<div class="card back">?</div>';return `<div class="card ${/[♥♦]/.test(c)?'red':''}">${esc(c)}</div>`}
function renderGame(){const g=state.game;$('#pokerBoard').classList.toggle('hidden',g!=='poker');$('#blackjackBoard').classList.toggle('hidden',g!=='blackjack');$('#rouletteBoard').classList.toggle('hidden',g!=='roulette');$('#chessBoard').classList.toggle('hidden',g!=='chess');$('#pokerActions').classList.toggle('hidden',g!=='poker');$('#bjActions').classList.toggle('hidden',g!=='blackjack');$('#pokerHelp')?.classList.toggle('hidden',g!=='poker');if(g==='poker')renderPoker();if(g==='blackjack')renderBJ();if(g==='roulette')renderRoulette();if(g==='chess')renderChess()}
function seatPos(i,n){const a=(-90+(360/n)*i)*Math.PI/180;return {left:50+43*Math.cos(a),top:50+43*Math.sin(a)}}
function renderPoker(){
 const p=state.poker||{}, players=state.players, mine=mePlayer(), myTurn=state.turnPlayerId===mine?.id;
 $('#pot').innerHTML=`POT <b>${(p.pot||0).toLocaleString()}</b><span class="street-label">${esc(p.phase)}</span>`;
 $('#community').innerHTML=(p.community||[]).map(card).join('');
 $('#seats').innerHTML=players.map((x,i)=>{const pos=seatPos(i,Math.max(2,players.length));const mineSeat=x.id===mine?.id;const hole=mineSeat&&state.me?.hole?.length?`<div class="hole">${state.me.hole.map(card).join('')}</div>`:'';return `<div class="seat ${x.folded?'folded ':''}${state.turnPlayerId===x.id?'active':''}" style="left:${pos.left}%;top:${pos.top}%;transform:translate(-50%,-50%)"><div class="seat-name">${esc(x.name)} ${x.bot?'🤖':''}</div><div class="seat-chips">${x.chips.toLocaleString()} · bet ${x.bet}</div><div class="seat-action">${esc(x.lastAction||'')}</div>${hole}</div>`}).join('');
 const winner=(p.winners||[]).map(w=>{const pl=players.find(x=>x.id===w.id);return `${esc(pl?.name||'Player')} +${w.amount.toLocaleString()} · ${esc(w.hand)}`}).join(' • ');$('#winner').innerHTML=winner;
 const turnName=players.find(x=>x.id===state.turnPlayerId)?.name;$('#turnBadge').textContent=myTurn?'YOUR TURN':turnName?`${esc(turnName)}'S TURN`:p.phase||'WAITING';updateTimer();
 const toCall=Math.max(0,(p.highestBet||0)-(mine?.bet||0));$('#callBtn').textContent=toCall?`CALL ${toCall}`:'CHECK';$$('#pokerActions [data-action]').forEach(b=>b.disabled=!myTurn);$('#raiseAmount').placeholder=`Raise to ${Math.max((p.highestBet||0)+(p.minRaise||50),(mine?.bet||0)+1)}`;$('#raiseAmount').disabled=!myTurn;
 const hint=$('#pokerHelp');if(hint)hint.innerHTML=myTurn?`<b>Your move:</b> ${toCall?`Match ${toCall} to stay in, or raise.`:'You may check or raise.'}`:`Waiting for ${esc(turnName||'next player')}…`;
}
function renderBJ(){const b=state.blackjack||{},mine=mePlayer();$('#dealerCards').innerHTML=(b.dealer||[]).map(card).join('');$('#bjHands').innerHTML=state.players.map(p=>{const h=b.hands?.[p.id];if(!h||!h.bet)return `<div class="bj-hand"><b>${esc(p.name)}</b><div class="muted">No bet</div></div>`;return `<div class="bj-hand ${state.turnPlayerId===p.id?'bj-active':''}"><b>${esc(p.name)} ${p.bot?'🤖':''}</b><div class="cards">${h.cards.map(card).join('')}</div><div>${h.total} · bet ${h.bet}</div><div class="eyebrow">${esc(h.result||(!h.done?'PLAYING':''))}</div></div>`}).join('');const myTurn=state.turnPlayerId===mine?.id;$$('[data-bj]').forEach(b=>b.disabled=!myTurn);$('#bjBetBtn').disabled=b.phase!=='BETTING'||!mine;const hint=$('#bjHelp');if(hint){if(b.phase==='BETTING')hint.innerHTML='<b>Betting open:</b> Enter an amount and press BET. You have ~10 seconds before cards are dealt.';else if(myTurn)hint.innerHTML='<b>Your turn:</b> Hit to draw, Stand to hold, or Double on your first two cards.';else hint.innerHTML='Waiting for other players or the dealer…'}}
function renderRoulette(){const r=state.roulette||{};$('#rouletteResult').textContent=r.result?`RESULT: ${r.result}`:r.phase==='SPINNING'?'SPINNING…':'PLACE YOUR BETS';$('#wheel').classList.toggle('spinning',r.phase==='SPINNING');const mine=mePlayer();$('#spinBtn').disabled=!mine?.host||r.phase!=='BETTING';const hint=$('#rouletteHelp');if(hint)hint.innerHTML=r.phase==='BETTING'?'Choose a bet type and amount. Even-money bets pay 1:1. Host presses SPIN when ready.':'Wheel is spinning…'}
const pieces={p:'♟',r:'♜',n:'♞',b:'♝',q:'♛',k:'♚',P:'♙',R:'♖',N:'♘',B:'♗',Q:'♕',K:'♔'};
function renderChess(){const c=state.chess||{},mine=mePlayer();const myColor=mine?.id===c.white?'w':mine?.id===c.black?'b':null;const myTurn=myColor&&c.turn===myColor;$('#chessStatus').textContent=c.winners?.length?'GAME OVER':`Turn: ${c.turn==='w'?'White':'Black'}${myTurn?' — your move':''}`;if(!c.fen)return;const rows=[];for(const row of c.fen.split(' ')[0].split('/')){const out=[];for(const ch of row){if(/\d/.test(ch))for(let i=0;i<+ch;i++)out.push(null);else out.push(ch)}rows.push(out)}$('#chessGrid').innerHTML=rows.flatMap((row,r)=>row.map((piece,col)=>{const sq=String.fromCharCode(97+col)+(8-r);return `<div class="sq ${(r+col)%2?'dark':'light'} ${selectedChess===sq?'sel':''}" data-sq="${sq}">${piece?pieces[piece]:''}</div>`})).join('');$$('#chessGrid .sq').forEach(x=>x.onclick=()=>chessClick(x.dataset.sq));const hint=$('#chessHelp');if(hint)hint.innerHTML=myTurn?'<b>Your move:</b> Click one of your pieces, then click where it should go.':'Waiting for opponent…'}
function updateTimer(){const el=$('#timer');if(!el)return;el.textContent=state?.turnEndsAt?`${Math.max(0,Math.ceil((state.turnEndsAt-Date.now())/1000))}s`:'—'}
function updateChipBalance(){const mine=mePlayer();const el=$('#chipBalance');if(el&&mine)el.textContent=`${mine.chips.toLocaleString()} chips`}
function maybeOfferRefill(){const mine=mePlayer();if(!mine||mine.bot||refillPromptOpen)return;if(state?.me?.refillAvailable){refillPromptOpen=true;openModal(`<div class="refill-modal"><span class="eyebrow">OUT OF CHIPS</span><h2>One-time bonus</h2><p class="modal-sub">You are at zero chips. Claim a single 1,000 chip bonus to keep playing.</p><div class="refill-chip">+1,000</div><button class="btn gold full" id="claimRefillBtn">CLAIM BONUS</button><button class="btn ghost full" id="dismissRefillBtn">Not now</button></div>`);$('#claimRefillBtn').onclick=async()=>{const r=await call('claimRefill',{});if(!r.ok){toast(r.error||'Could not claim bonus');return}closeModal();refillPromptOpen=false;toast('You received 1,000 bonus chips!')};$('#dismissRefillBtn').onclick=()=>{closeModal();refillPromptOpen=false}}}
function chessClick(sq){if(!selectedChess){selectedChess=sq;renderChess();return}const from=selectedChess;selectedChess=null;let promotion;if(from[1]==='7'&&sq[1]==='8'||from[1]==='2'&&sq[1]==='1')promotion='q';call('chessMove',{move:{from,to:sq,promotion}}).then(r=>{if(!r.ok)toast(r.error||'Illegal move')});renderChess()}
function nameModal(join){openModal(`<span class="eyebrow">${join?'JOIN ROOM':'CREATE ROOM'}</span><h2>${join?'Join a room':'Choose your name'}</h2><p class="modal-sub">Your name is saved on this device.</p>${join?'<label class="modal-label">ROOM CODE</label><input id="joinCode" class="modal-input" maxlength="6" placeholder="ABC123">':''}<label class="modal-label">YOUR NAME</label><input id="playerName" class="modal-input" maxlength="18" placeholder="Your name" value="${esc(savedName())}" autocomplete="nickname"><button class="btn primary full" id="modalSubmit">${join?'JOIN ROOM':'CREATE ROOM'}</button>`);setTimeout(()=>$('#playerName')?.focus(),30)}
$('#createBtn').onclick=()=>nameModal(false);$('#joinBtn').onclick=()=>nameModal(true);$('#modalClose').onclick=closeModal;$('#modal').onclick=e=>{if(e.target.id==='modal')closeModal()};
document.addEventListener('click',e=>{if(e.target.id==='modalSubmit'){const name=rememberName($('#playerName').value);if(!name)return toast('Enter your name.');if($('#joinCode')){const code=$('#joinCode').value.trim().toUpperCase();if(code.length!==6)return toast('Enter the 6-character room code.');call('joinRoom',{code,name}).then(r=>{if(!r.ok)toast(r.error);else closeModal()})}else call('createRoom',{name}).then(r=>{if(!r.ok)toast(r.error);else{me={reconnectToken:r.reconnectToken};roomCode=r.code;saveSession();closeModal()}})}});
$$('.game-tab').forEach(b=>b.onclick=()=>call('setGame',{game:b.dataset.game}).then(r=>{if(!r.ok)toast(r.error)}));
$('#startBtn').onclick=async()=>{const r=await call('startGame',{});if(!r.ok)toast(r.error);};
$('#addBotBtn').onclick=()=>openModal(`<span class="eyebrow">SERVER-SIDE BOTS</span><h2>Add a bot</h2><p class="modal-sub">Pick a personality and tap Add. Bots play automatically on the server — add one, then press Start Game.</p><div class="bot-grid">${Object.entries({Ace:'Tight / conservative',Shark:'Aggressive / pressure',Bluff:'Frequent bluffs',Lucky:'Loose / call-heavy',Dealer:'Low variance',River:'Balanced',Pocket:'Tight-aggressive',Wildcard:'Unpredictable'}).map(([n,d])=>`<button class="bot-option" data-bot="${n}"><b>${n}</b><span>${d}</span></button>`).join('')}</div>`);
document.addEventListener('click',e=>{const b=e.target.closest('[data-bot]');if(b)call('addBot',{type:b.dataset.bot}).then(r=>{if(!r.ok)toast(r.error);else{closeModal();toast(`${b.dataset.bot} bot added — press Start Game`)}})});
$$('[data-action]').forEach(b=>b.onclick=()=>{let amount=Number($('#raiseAmount').value);call('pokerAction',{action:b.dataset.action,amount}).then(r=>{if(!r.ok)toast(r.error)})});
$$('[data-bj]').forEach(b=>b.onclick=()=>call('blackjackAction',{action:b.dataset.bj}).then(r=>{if(!r.ok)toast(r.error)}));$('#bjBetBtn').onclick=()=>call('blackjackBet',{amount:Number($('#bjBet').value)}).then(r=>{if(!r.ok)toast(r.error)});
$('#spinBtn').onclick=()=>call('rouletteSpin',{}).then(r=>{if(!r.ok)toast(r.error)});
$$('.bet-chip').forEach(b=>b.onclick=()=>{const amount=Number($('#rouletteAmount').value||25);call('rouletteBet',{type:b.dataset.type,value:b.dataset.value||'',amount}).then(r=>{if(!r.ok)toast(r.error);else toast('Bet placed')})});
async function leave(){const r=await call('leaveRoom',{});if(!r.ok)return toast(r.error);localStorage.removeItem('gh_session');state=null;me=null;roomCode='';refillPromptOpen=false;showScreen('home')}
$('#leaveBtn').onclick=leave;$('#leaveLobbyBtn').onclick=leave;$('#copyRoom').onclick=()=>navigator.clipboard?.writeText(roomCode).then(()=>toast('Room code copied'));
function chat(form,input){$(form).onsubmit=e=>{e.preventDefault();const v=$(input).value.trim();if(v)call('chat',{text:v});$(input).value=''}}chat('#chatForm','#chatInput');chat('#gameChatForm','#gameChatInput');
setInterval(()=>{if(state?.turnEndsAt)updateTimer()},500);document.addEventListener('keydown',e=>{if(e.key==='Escape')closeModal()});
